import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  responseImc: {
    flex: 1,
    marginTop: 15,
    borderRadius: 50,
    alignItems: "center",
    width: "100%",
  },
  informationResult: {
    fontSize: 20,
    color: "#2C3E50",
    fontWeight: "Bold",
  },

  numberResult: {
    fontSize: 35,
    color: "#F39C12",
    fontWeight: "700",
  },
});

export default styles;
